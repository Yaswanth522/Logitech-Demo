# Ask Logi needs ChatWidget.send/prewarm/on, which only exist on the staging
# widget build until the headless-send change ships to prod (web-app#4777).
# Force staging here too so the deployed site behaves like local dev instead
# of silently timing out on every search. Remove this once prod has the API.
VITE_YM_WIDGET_CHANNEL=staging
